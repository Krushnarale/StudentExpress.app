package com.core2web.view;

import javafx.scene.Node;
import javafx.scene.layout.VBox;

/**
 * Wallet feature removed completely as per spec.
 */
public class WalletPage {
    public Node getPageNode(Runnable onBack) {
        return new VBox();
    }
}
